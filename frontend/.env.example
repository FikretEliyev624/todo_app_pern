# Local dev: leave empty; vite.config.js proxies /api → http://localhost:4000
# Production (Vercel): set this to the backend's deployed URL + /api
#   e.g. VITE_API_BASE=https://notepad-todo-api.vercel.app/api
VITE_API_BASE=/api
